'use client';

import { useState } from 'react';

export function NewsletterSection() {
  // Since we've moved the newsletter to the footer, this component can be empty
  // or we can return null to not render anything
  return null;
}